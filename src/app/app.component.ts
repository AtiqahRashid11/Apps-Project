import { Component } from '@angular/core';
// import {AurtService } from './auth.service';

@Component({
  selector: 'app-root',
  templateUrl: 'app.component.html',
  styleUrls: ['app.component.scss'],
})
export class AppComponent {
  title = 'app';
}

// constructor(private_authService: authService){}